window.HANDSON_DATA_MOD_T2 = [
  {
    "id": 2701,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #1).",
    "question": "[MOD Coding Task #1] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2702,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #2).",
    "question": "[MOD Coding Task #2] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Chat Completions Client (`get_chat_completions_client`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Chat Completions Client (`get_chat_completions_client`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2703,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #3).",
    "question": "[MOD Coding Task #3] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2704,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #4).",
    "question": "[MOD Coding Task #4] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `handling tool call execution inside chat loop`? (Select three)",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `handling tool call execution inside chat loop`, enterprise coding standards mandate: (1) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2705,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #5).",
    "question": "[MOD Coding Task #5] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2706,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #6).",
    "question": "[MOD Coding Task #6] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Prompt Template Engine (`prompts/trail_guide.jinja2`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Prompt Template Engine (`prompts/trail_guide.jinja2`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2707,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #7).",
    "question": "[MOD Coding Task #7] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2708,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #8).",
    "question": "[MOD Coding Task #8] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `handling tool call execution inside chat loop`? (Select three)",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `handling tool call execution inside chat loop`, enterprise coding standards mandate: (1) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2709,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #9).",
    "question": "[MOD Coding Task #9] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2710,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling tool call execution inside chat loop` (Implementation Task #10).",
    "question": "[MOD Coding Task #10] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Model Deployment Discovery (`list_models`, `get_deployment`)` when implementing `handling tool call execution inside chat loop`?",
    "options": [
      "A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Model Deployment Discovery (`list_models`, `get_deployment`)`, Writing code that checks `if response.choices[0].message.tool_calls:`, iterates over tool calls, runs local functions, appends `role: 'tool'` messages, and calls `chat.completions.create()` again. is the officially required syntax for `handling tool call execution inside chat loop`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2711,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #11).",
    "question": "[MOD Coding Task #11] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2712,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #12).",
    "question": "[MOD Coding Task #12] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `loading and rendering Jinja2 prompt files dynamically`? (Select three)",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `loading and rendering Jinja2 prompt files dynamically`, enterprise coding standards mandate: (1) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2713,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #13).",
    "question": "[MOD Coding Task #13] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2714,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #14).",
    "question": "[MOD Coding Task #14] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2715,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #15).",
    "question": "[MOD Coding Task #15] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2716,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #16).",
    "question": "[MOD Coding Task #16] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `loading and rendering Jinja2 prompt files dynamically`? (Select three)",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `loading and rendering Jinja2 prompt files dynamically`, enterprise coding standards mandate: (1) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2717,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #17).",
    "question": "[MOD Coding Task #17] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2718,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #18).",
    "question": "[MOD Coding Task #18] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2719,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #19).",
    "question": "[MOD Coding Task #19] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `loading and rendering Jinja2 prompt files dynamically`?",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`. is the officially required syntax for `loading and rendering Jinja2 prompt files dynamically`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2720,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `loading and rendering Jinja2 prompt files dynamically` (Implementation Task #20).",
    "question": "[MOD Coding Task #20] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Model Deployment Discovery (`list_models`, `get_deployment`)` for `loading and rendering Jinja2 prompt files dynamically`? (Select three)",
    "options": [
      "A) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Model Deployment Discovery (`list_models`, `get_deployment`)` for `loading and rendering Jinja2 prompt files dynamically`, enterprise coding standards mandate: (1) Executing `from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('prompts')); prompt = env.get_template('trail_guide.jinja2').render(user_name='Alice')`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2721,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #21).",
    "question": "[MOD Coding Task #21] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2722,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #22).",
    "question": "[MOD Coding Task #22] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Chat Completions Client (`get_chat_completions_client`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Chat Completions Client (`get_chat_completions_client`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2723,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #23).",
    "question": "[MOD Coding Task #23] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2724,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #24).",
    "question": "[MOD Coding Task #24] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `implementing exponential backoff for `HttpResponseError` 429`? (Select three)",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `implementing exponential backoff for `HttpResponseError` 429`, enterprise coding standards mandate: (1) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2725,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #25).",
    "question": "[MOD Coding Task #25] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2726,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #26).",
    "question": "[MOD Coding Task #26] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Prompt Template Engine (`prompts/trail_guide.jinja2`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Prompt Template Engine (`prompts/trail_guide.jinja2`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2727,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #27).",
    "question": "[MOD Coding Task #27] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2728,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #28).",
    "question": "[MOD Coding Task #28] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `implementing exponential backoff for `HttpResponseError` 429`? (Select three)",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `implementing exponential backoff for `HttpResponseError` 429`, enterprise coding standards mandate: (1) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2729,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #29).",
    "question": "[MOD Coding Task #29] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2730,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `implementing exponential backoff for `HttpResponseError` 429` (Implementation Task #30).",
    "question": "[MOD Coding Task #30] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Model Deployment Discovery (`list_models`, `get_deployment`)` when implementing `implementing exponential backoff for `HttpResponseError` 429`?",
    "options": [
      "A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Model Deployment Discovery (`list_models`, `get_deployment`)`, Writing `@retry(retry=retry_if_exception_type(HttpResponseError), stop=stop_after_attempt(5), wait=wait_exponential(multiplier=1, min=2, max=60))` around SDK inference calls. is the officially required syntax for `implementing exponential backoff for `HttpResponseError` 429`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2731,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #31).",
    "question": "[MOD Coding Task #31] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2732,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #32).",
    "question": "[MOD Coding Task #32] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `iterating over SSE streaming chat completion chunks`? (Select three)",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `iterating over SSE streaming chat completion chunks`, enterprise coding standards mandate: (1) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2733,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #33).",
    "question": "[MOD Coding Task #33] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2734,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #34).",
    "question": "[MOD Coding Task #34] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2735,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #35).",
    "question": "[MOD Coding Task #35] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2736,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #36).",
    "question": "[MOD Coding Task #36] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `iterating over SSE streaming chat completion chunks`? (Select three)",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `iterating over SSE streaming chat completion chunks`, enterprise coding standards mandate: (1) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2737,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #37).",
    "question": "[MOD Coding Task #37] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2738,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #38).",
    "question": "[MOD Coding Task #38] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2739,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #39).",
    "question": "[MOD Coding Task #39] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `iterating over SSE streaming chat completion chunks`?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`. is the officially required syntax for `iterating over SSE streaming chat completion chunks`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2740,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `iterating over SSE streaming chat completion chunks` (Implementation Task #40).",
    "question": "[MOD Coding Task #40] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Model Deployment Discovery (`list_models`, `get_deployment`)` for `iterating over SSE streaming chat completion chunks`? (Select three)",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Model Deployment Discovery (`list_models`, `get_deployment`)` for `iterating over SSE streaming chat completion chunks`, enterprise coding standards mandate: (1) Writing `response = chat_client.chat.completions.create(..., stream=True); [print(chunk.choices[0].delta.content or '', end='') for chunk in response]`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2741,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #41).",
    "question": "[MOD Coding Task #41] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2742,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #42).",
    "question": "[MOD Coding Task #42] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Chat Completions Client (`get_chat_completions_client`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Chat Completions Client (`get_chat_completions_client`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2743,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #43).",
    "question": "[MOD Coding Task #43] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2744,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #44).",
    "question": "[MOD Coding Task #44] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `creating an Assistant Agent with vector store attachment`? (Select three)",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `creating an Assistant Agent with vector store attachment`, enterprise coding standards mandate: (1) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2745,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #45).",
    "question": "[MOD Coding Task #45] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2746,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #46).",
    "question": "[MOD Coding Task #46] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Prompt Template Engine (`prompts/trail_guide.jinja2`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Prompt Template Engine (`prompts/trail_guide.jinja2`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2747,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #47).",
    "question": "[MOD Coding Task #47] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2748,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #48).",
    "question": "[MOD Coding Task #48] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `creating an Assistant Agent with vector store attachment`? (Select three)",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `creating an Assistant Agent with vector store attachment`, enterprise coding standards mandate: (1) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2749,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #49).",
    "question": "[MOD Coding Task #49] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2750,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `creating an Assistant Agent with vector store attachment` (Implementation Task #50).",
    "question": "[MOD Coding Task #50] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Model Deployment Discovery (`list_models`, `get_deployment`)` when implementing `creating an Assistant Agent with vector store attachment`?",
    "options": [
      "A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Model Deployment Discovery (`list_models`, `get_deployment`)`, Writing `agent = project_client.agents.create_agent(model='gpt-4o', name='trail-guide', tool_resources={'file_search': {'vector_store_ids': [vector_store.id]}})`. is the officially required syntax for `creating an Assistant Agent with vector store attachment`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2751,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #51).",
    "question": "[MOD Coding Task #51] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2752,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #52).",
    "question": "[MOD Coding Task #52] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `retrieving telemetry connection string via SDK property`? (Select three)",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `retrieving telemetry connection string via SDK property`, enterprise coding standards mandate: (1) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2753,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #53).",
    "question": "[MOD Coding Task #53] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2754,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #54).",
    "question": "[MOD Coding Task #54] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2755,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #55).",
    "question": "[MOD Coding Task #55] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2756,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #56).",
    "question": "[MOD Coding Task #56] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `retrieving telemetry connection string via SDK property`? (Select three)",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `retrieving telemetry connection string via SDK property`, enterprise coding standards mandate: (1) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2757,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #57).",
    "question": "[MOD Coding Task #57] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2758,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #58).",
    "question": "[MOD Coding Task #58] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2759,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #59).",
    "question": "[MOD Coding Task #59] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `retrieving telemetry connection string via SDK property`?",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters. is the officially required syntax for `retrieving telemetry connection string via SDK property`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2760,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `retrieving telemetry connection string via SDK property` (Implementation Task #60).",
    "question": "[MOD Coding Task #60] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Model Deployment Discovery (`list_models`, `get_deployment`)` for `retrieving telemetry connection string via SDK property`? (Select three)",
    "options": [
      "A) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Model Deployment Discovery (`list_models`, `get_deployment`)` for `retrieving telemetry connection string via SDK property`, enterprise coding standards mandate: (1) Executing `telemetry_conn = project_client.telemetry.get_connection_string()` to pass directly into OpenTelemetry trace exporters., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2761,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #61).",
    "question": "[MOD Coding Task #61] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2762,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #62).",
    "question": "[MOD Coding Task #62] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Chat Completions Client (`get_chat_completions_client`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Chat Completions Client (`get_chat_completions_client`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2763,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #63).",
    "question": "[MOD Coding Task #63] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2764,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #64).",
    "question": "[MOD Coding Task #64] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `listing all active deployed models in the project`? (Select three)",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `listing all active deployed models in the project`, enterprise coding standards mandate: (1) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2765,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #65).",
    "question": "[MOD Coding Task #65] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2766,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #66).",
    "question": "[MOD Coding Task #66] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Prompt Template Engine (`prompts/trail_guide.jinja2`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Prompt Template Engine (`prompts/trail_guide.jinja2`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2767,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #67).",
    "question": "[MOD Coding Task #67] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2768,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #68).",
    "question": "[MOD Coding Task #68] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `listing all active deployed models in the project`? (Select three)",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `listing all active deployed models in the project`, enterprise coding standards mandate: (1) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2769,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #69).",
    "question": "[MOD Coding Task #69] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2770,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `listing all active deployed models in the project` (Implementation Task #70).",
    "question": "[MOD Coding Task #70] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Model Deployment Discovery (`list_models`, `get_deployment`)` when implementing `listing all active deployed models in the project`?",
    "options": [
      "A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Model Deployment Discovery (`list_models`, `get_deployment`)`, Executing `deployments = project_client.inference.list_deployments()` and iterating to verify `gpt-4o` and `text-embedding-3-large` status. is the officially required syntax for `listing all active deployed models in the project`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2771,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #71).",
    "question": "[MOD Coding Task #71] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2772,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #72).",
    "question": "[MOD Coding Task #72] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `passing structured JSON schema via `response_format``? (Select three)",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `passing structured JSON schema via `response_format``, enterprise coding standards mandate: (1) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2773,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #73).",
    "question": "[MOD Coding Task #73] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2774,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #74).",
    "question": "[MOD Coding Task #74] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2775,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #75).",
    "question": "[MOD Coding Task #75] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2776,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #76).",
    "question": "[MOD Coding Task #76] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `passing structured JSON schema via `response_format``? (Select three)",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `passing structured JSON schema via `response_format``, enterprise coding standards mandate: (1) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2777,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #77).",
    "question": "[MOD Coding Task #77] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2778,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #78).",
    "question": "[MOD Coding Task #78] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2779,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #79).",
    "question": "[MOD Coding Task #79] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `passing structured JSON schema via `response_format``?",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON. is the officially required syntax for `passing structured JSON schema via `response_format``. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2780,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `passing structured JSON schema via `response_format`` (Implementation Task #80).",
    "question": "[MOD Coding Task #80] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Model Deployment Discovery (`list_models`, `get_deployment`)` for `passing structured JSON schema via `response_format``? (Select three)",
    "options": [
      "A) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Model Deployment Discovery (`list_models`, `get_deployment`)` for `passing structured JSON schema via `response_format``, enterprise coding standards mandate: (1) Writing `response = chat_client.chat.completions.create(..., response_format={'type': 'json_object'})` after instructing the model in `system` prompt to output valid JSON., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2781,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #81).",
    "question": "[MOD Coding Task #81] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2782,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #82).",
    "question": "[MOD Coding Task #82] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Chat Completions Client (`get_chat_completions_client`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Chat Completions Client (`get_chat_completions_client`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2783,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #83).",
    "question": "[MOD Coding Task #83] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2784,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #84).",
    "question": "[MOD Coding Task #84] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `handling authentication token expiration seamlessly`? (Select three)",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` for `handling authentication token expiration seamlessly`, enterprise coding standards mandate: (1) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2785,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #85).",
    "question": "[MOD Coding Task #85] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2786,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #86).",
    "question": "[MOD Coding Task #86] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Prompt Template Engine (`prompts/trail_guide.jinja2`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Prompt Template Engine (`prompts/trail_guide.jinja2`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2787,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #87).",
    "question": "[MOD Coding Task #87] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2788,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #88).",
    "question": "[MOD Coding Task #88] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `handling authentication token expiration seamlessly`? (Select three)",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` for `handling authentication token expiration seamlessly`, enterprise coding standards mandate: (1) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2789,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #89).",
    "question": "[MOD Coding Task #89] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2790,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `handling authentication token expiration seamlessly` (Implementation Task #90).",
    "question": "[MOD Coding Task #90] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Model Deployment Discovery (`list_models`, `get_deployment`)` when implementing `handling authentication token expiration seamlessly`?",
    "options": [
      "A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Model Deployment Discovery (`list_models`, `get_deployment`)`, Writing a custom `TokenRefreshPolicy` inside `AIProjectClient` pipeline that intercepts `401 Unauthorized` responses and re-runs `credential.get_token('https://cognitiveservices.azure.com/.default')`. is the officially required syntax for `handling authentication token expiration seamlessly`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2791,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Adventure Works Core Hub` implementing ``azure.ai.projects.AIProjectClient` Initialization`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #91).",
    "question": "[MOD Coding Task #91] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for ``azure.ai.projects.AIProjectClient` Initialization` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for ``azure.ai.projects.AIProjectClient` Initialization`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2792,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Contoso Health AI` implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #92).",
    "question": "[MOD Coding Task #92] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `attaching custom request headers to SDK calls`? (Select three)",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `OpenAI Chat Completions Client (`get_chat_completions_client`)` for `attaching custom request headers to SDK calls`, enterprise coding standards mandate: (1) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2793,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Fabrikam Logistics Agent` implementing `OpenAI Embeddings Client (`get_embeddings_client`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #93).",
    "question": "[MOD Coding Task #93] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenAI Embeddings Client (`get_embeddings_client`)` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenAI Embeddings Client (`get_embeddings_client`)`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2794,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Trey Research Lab` implementing `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #94).",
    "question": "[MOD Coding Task #94] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Agent Tool Calling Payload (`tools=[{'type': 'function'...}]`)`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2795,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Northwind Traders Bot` implementing `File Upload & Agent Vector Store (`project_client.agents`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #95).",
    "question": "[MOD Coding Task #95] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `File Upload & Agent Vector Store (`project_client.agents`)` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `File Upload & Agent Vector Store (`project_client.agents`)`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2796,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Woodgrove Bank Virtual Assistant` implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #96).",
    "question": "[MOD Coding Task #96] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `attaching custom request headers to SDK calls`? (Select three)",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Prompt Template Engine (`prompts/trail_guide.jinja2`)` for `attaching custom request headers to SDK calls`, enterprise coding standards mandate: (1) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  },
  {
    "id": 2797,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Litware Legal Evaluator` implementing `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #97).",
    "question": "[MOD Coding Task #97] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `HTTP Rate Limit Handling (`@retry`, `HttpResponseError`)`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2798,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `A.Datum Retail Copilot` implementing `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #98).",
    "question": "[MOD Coding Task #98] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `OpenTelemetry Tracing Integration (`configure_azure_monitor`)`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2799,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "single",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Alpine Ski Field Technician` implementing `Streaming Response Handling (`stream=True`, `SSE`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #99).",
    "question": "[MOD Coding Task #99] Which of the following represents the exact, correct Python SDK v2, Bicep IaC, KQL, or GitHub Actions YAML syntax for `Streaming Response Handling (`stream=True`, `SSE`)` when implementing `attaching custom request headers to SDK calls`?",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Using deprecated Python SDK v1 (`azureml-sdk`) workspace syntax (`Workspace.from_config()`) or unencrypted public endpoints without enabling `DefaultAzureCredential()`.",
      "C) Hardcoding plaintext connection strings and API keys directly inside inline HTML or local `.env` files without Key Vault RBAC integration.",
      "D) Disabling diagnostic log forwarding (`retentionsInDays: 0`) and setting `publicNetworkAccess: 'Enabled'` across all AI Foundry data stores."
    ],
    "correctAnswer": [
      0
    ],
    "explanation": "Correct Answer: A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.. Explanation: In Microsoft AI-300 enterprise hands-on coding for `Streaming Response Handling (`stream=True`, `SSE`)`, Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation. is the officially required syntax for `attaching custom request headers to SDK calls`. Why others are incorrect: Options B, C, and D reference deprecated SDK v1 methods, insecure plaintext credential exposure, or non-compliant public network exposure."
  },
  {
    "id": 2800,
    "domain": "Track 2: Python SDK v2 & Azure AI Foundry Client",
    "type": "multiple",
    "scenario": "You are a Senior MLOps & GenAI coding engineer at `Tailwind Traders Multi-Agent` implementing `Model Deployment Discovery (`list_models`, `get_deployment`)`. Your team needs to verify the exact code syntax, CLI flag, or configuration block required for `attaching custom request headers to SDK calls` (Implementation Task #100).",
    "question": "[MOD Coding Task #100] Which THREE of the following represent mandatory code blocks, exact SDK parameters, or architectural requirements when configuring `Model Deployment Discovery (`list_models`, `get_deployment`)` for `attaching custom request headers to SDK calls`? (Select three)",
    "options": [
      "A) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation.",
      "B) Authenticating via zero-trust `DefaultAzureCredential()` (`User-Assigned Managed Identity`) or OIDC workload identity federation (`id-token: write`).",
      "C) Enabling structured diagnostic log forwarding (`AgentTelemetry`, `AppDependencies`) directly to Log Analytics for continuous KQL tracing.",
      "D) Disabling all Private Endpoints (`publicNetworkAccess: 'Enabled'`) and exposing model inference ports directly to public `0.0.0.0/0` internet traffic.",
      "E) Storing production OpenAI `api-key` strings directly inside client-side frontend bundle files to bypass Key Vault secret retrieval overhead."
    ],
    "correctAnswer": [
      0,
      1,
      2
    ],
    "explanation": "Correct Answers: A, B, and C. Explanation: When implementing `Model Deployment Discovery (`list_models`, `get_deployment`)` for `attaching custom request headers to SDK calls`, enterprise coding standards mandate: (1) Passing `headers={'x-ms-client-request-id': str(uuid.uuid4()), 'x-ms-user-id': 'emp-1029'}` into SDK client calls for end-to-end audit correlation., (2) zero-trust authentication (`DefaultAzureCredential` / `OIDC`), and (3) continuous diagnostic telemetry forwarding to Log Analytics. Why others are incorrect: Options D and E introduce critical zero-day security vulnerabilities (`public exposure`, `hardcoded secrets`)."
  }
];
